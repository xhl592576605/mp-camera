# CONTEXT.md — 领域词汇表

> 水印相机项目的领域概念，供架构讨论和代码导航使用。

## 核心概念

- **快照 (Snapshot)** — 编辑器中图片的历史状态，包含 `{ tempFilePath, width, height }`。快照栈支持撤销/重做。
- **插件 (Plugin)** — 编辑器工具的实现单元（裁剪、旋转、画笔、贴纸、文字）。遵循统一接口：生命周期、渲染、提交、触摸、面板。
- **编排器 (Orchestrator)** — `useEditor` 的核心角色，管理插件生命周期、快照栈、画布、工具切换。不包含工具专属逻辑。
- **图片区域 (Image Rect)** — 图片在 Canvas 中适配后的矩形区域 `{ x, y, w, h }`，所有坐标映射的基准。
- **贴纸抽屉 (Sticker Drawer)** — 贴纸选择面板，支持分类折叠、表情/图片两种类型。
- **选择框 (Selection Frame)** — 贴纸/文字被选中时显示的操作框，包含删除、翻转、变换按钮。
- **手势变换 (Gesture Transform)** — 拖动、旋转、缩放贴纸/文字元素的触摸交互，基于 hit-to-local 坐标反变换。

## 架构概念（架构评审引入）

- **CanvasContext** — 注入给插件的结构化对象，封装画布访问、图片信息、导出管道、选择状态。替代原先 21 个隐式回调。
- **导出管道 (Export Pipeline)** — 共享的 Canvas 导出流程：创建导出画布 → 可选绘制原图 → 插件渲染 → 导出文件。
- **手势工具 (Gesture Helper)** — 共享的触摸交互工具函数：坐标反变换、碰撞检测、选择框绘制。位于 `src/utils/gesture-helper.js`。
- **画布代理 (Canvas Proxy)** — 编排器在调用插件渲染时自动包裹 `save()`/`restore()`，确保插件不会污染画布状态。

## 页面

- **相机页** — 取景、拍照/录像、闪光灯、水印合成
- **编辑器** — 图片编辑，基于插件系统
- **预览页** — 图片/视频预览、保存到相册
